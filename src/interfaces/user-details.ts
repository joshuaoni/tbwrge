export interface UserResponse {
  id: string;
  email: string;
  profile_picture: string;
  username: string;
  createdAt: string;
  updatedAt: string;
  name: string;
  role: string;
  joined_talent_pool: boolean;
  plan: string;
  plan_expires: string;
}
